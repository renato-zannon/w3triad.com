<%= form_for post do |f| %>
  <%= f.label :title, t(:title) %><br>
  <%= f.text_field :title %><br>

  <%= f.label :content, t(:content) %><br>
  <%= f.text_area :content, :size => "100x20" %><br>

  <%= f.submit t(:save_post) %>
  <%= f.submit t(:preview), :name => "preview_button" %>
<% end %>
